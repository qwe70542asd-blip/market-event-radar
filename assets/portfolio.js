(async()=>{"use strict";const {$,escapeHtml,fmt,cls,finite,loadData,loadPortfolio,savePortfolio}=MR;const [assets,tw,global]=await Promise.all([loadData("assets.json",window.__ASSET_SEED__||{assets:[]}),loadData("tw-market.json",window.__TW_MARKET_SEED__||{items:[]}),loadData("market-snapshot.json",window.__MARKET_SNAPSHOT_SEED__||{items:[]})]);const quotes=new Map([...(tw.items||[]),...(global.items||[])].map(x=>[String(x.symbol).toUpperCase(),x]));let rows=loadPortfolio();
function render(){let totalCost=0,totalValue=0,valuedRows=0;$("#portfolioRows").innerHTML=rows.map((h,i)=>{const q=quotes.get(String(h.symbol).toUpperCase()),price=finite(q?.price),qty=Number(h.quantity||0),cost=Number(h.cost||0),value=price==null?null:price*qty,pl=value==null?null:value-cost*qty;totalCost+=cost*qty;if(value!=null){totalValue+=value;valuedRows++}return`<tr><td><a href="asset.html?symbol=${encodeURIComponent(h.symbol)}"><b>${escapeHtml(h.symbol)}</b><br><small>${escapeHtml(h.name||"")}</small></a></td><td>${fmt(qty,4)}</td><td>${fmt(cost)}</td><td>${fmt(price)}</td><td>${fmt(value,0)}</td><td class="${cls(pl)}">${fmt(pl,0)}</td><td><button class="btn" data-del="${i}">刪除</button></td></tr>`}).join("")||'<tr><td colspan="7" class="empty">尚未加入標的</td></tr>';const complete=rows.length>0&&valuedRows===rows.length;$("#totalCost").textContent=rows.length?fmt(totalCost,0):"—";$("#totalValue").textContent=complete?fmt(totalValue,0):"—";$("#totalPL").textContent=complete?fmt(totalValue-totalCost,0):"—";$("#totalPL").className=complete?cls(totalValue-totalCost):"flat";document.querySelectorAll("[data-del]").forEach(b=>b.onclick=()=>{rows.splice(Number(b.dataset.del),1);savePortfolio(rows);render()})}
$("#addHolding").onclick=()=>{const symbol=$("#holdingSymbol").value.trim().toUpperCase(),quantity=Number($("#holdingQty").value),cost=Number($("#holdingCost").value);if(!symbol||!Number.isFinite(quantity)||quantity<=0||!Number.isFinite(cost)){alert("請填入正確代碼、數量與成本");return}const a=(assets.assets||[]).find(x=>String(x.symbol).toUpperCase()===symbol);rows.push({symbol,name:$("#holdingName").value.trim()||a?.name||symbol,quantity,cost});savePortfolio(rows);render();["holdingSymbol","holdingName","holdingQty","holdingCost"].forEach(id=>$("#"+id).value="")};$("#exportPortfolio").onclick=()=>{const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([JSON.stringify({version:2,items:rows},null,2)],{type:"application/json"}));a.download="market-radar-portfolio.json";a.click()};$("#importPortfolio").onchange=async e=>{try{const js=JSON.parse(await e.target.files[0].text());rows=Array.isArray(js)?js:js.items;savePortfolio(rows);render()}catch(err){alert("匯入檔格式錯誤")}};render();})();
