(async()=>{
  "use strict";
  const {$,escapeHtml,formatTime,loadData}=MR;
  const BAD=new Set(["failed","error","unavailable","circuit-open"]),WARN=new Set(["partial","degraded","stale","pending","warning","fallback","loading","waiting","seeded","seed"]);
  const label=status=>({fresh:"正常",partial:"部分資料",degraded:"來源異常",stale:"資料過期",pending:"等待更新",failed:"失敗",unavailable:"無資料"}[status]||status||"未知");
  const css=status=>BAD.has(status)?"badge-warn":WARN.has(status)?"badge-warn":"badge-ok";
  const bytes=value=>{const n=Number(value);if(!Number.isFinite(n)||n<=0)return"—";if(n<1024)return`${n} B`;if(n<1024**2)return`${(n/1024).toFixed(1)} KB`;return`${(n/1024**2).toFixed(2)} MB`};
  const age=value=>{const n=Number(value);if(!Number.isFinite(n))return"—";if(n<60)return`${n} 秒`;if(n<3600)return`${Math.round(n/60)} 分`;if(n<86400)return`${(n/3600).toFixed(n<10800?1:0)} 小時`;return`${(n/86400).toFixed(1)} 天`};
  const reasons=row=>(row.reasons||[]).slice(0,3).join("；")||"—";
  const empty={metadata:{version:"v11.5.0",status:"unavailable",counts:{}},channels:[]};
  let payload=empty;
  try{payload=await loadData("channel-health.json",empty,{force:true})}catch(error){payload={...empty,metadata:{...empty.metadata,error:String(error)}}}
  const meta=payload.metadata||{},counts=meta.counts||{},verification=meta.verification_summary||{},channels=Array.isArray(payload.channels)?payload.channels:[];
  const mount=document.querySelector(".data-quality-summary"),trust=verification.trust_counts||{},complete=verification.completeness_counts||{},coverage=Number(verification.average_field_coverage_percent);
  const criticalBad=Number(meta.critical_bad_count||0),criticalWarn=Number(meta.critical_warning_count||0),criticalTone=criticalBad?"down":criticalWarn?"flat":"up";
  const summary=`<section class="panel content data-quality-summary"><div class="section-head"><div><p class="eyebrow">SEALED DATA HEALTH</p><h2>資料健康摘要</h2></div><small>${escapeHtml(formatTime(meta.updated_at))}</small></div><div class="stat-grid"><article class="stat"><small>核心資料</small><strong class="${criticalTone}">${criticalBad?`異常 ${criticalBad}`:criticalWarn?`待確認 ${criticalWarn}`:"正常"}</strong></article><article class="stat"><small>正常通道</small><strong>${Number(counts.fresh||0)}</strong></article><article class="stat"><small>部分／異常</small><strong class="${Number(meta.bad_count||0)?"down":"flat"}">${Number(meta.bad_count||0)}</strong></article><article class="stat"><small>等待更新</small><strong>${Number(counts.pending||0)}</strong></article><article class="stat"><small>過期來源</small><strong class="${Number(counts.stale||0)?"down":"flat"}">${Number(counts.stale||0)}</strong></article><article class="stat"><small>平均欄位覆蓋</small><strong>${Number.isFinite(coverage)?coverage.toFixed(1)+"%":"—"}</strong></article><article class="stat"><small>資料衝突</small><strong class="${Number(trust.conflict||0)?"down":"flat"}">${Number(trust.conflict||0).toLocaleString("zh-TW")}</strong></article><article class="stat"><small>欄位完整</small><strong>${Number(complete.complete||0).toLocaleString("zh-TW")}</strong></article><article class="stat"><small>部分完整</small><strong>${Number(complete.partial||0).toLocaleString("zh-TW")}</strong></article></div><p class="status-warning">此頁只下載小型健康索引。過期、不同交易日或來源降級會明確標示；不以「Workflow 成功」取代資料新鮮度判斷。</p></section>`;
  if(mount)mount.outerHTML=summary;else $("#channelGrid").insertAdjacentHTML("beforebegin",summary);
  $("#channelGrid").innerHTML=channels.map(row=>{const status=String(row.status||"pending").toLowerCase(),details=[`筆數：${Number(row.item_count||0).toLocaleString("zh-TW")}`,`更新：${formatTime(row.updated_at)}`,`資料年齡：${age(row.age_seconds)}`,`前端檔案：${bytes(row.payload_bytes)}`];if(row.stale_item_count)details.push(`過期項目：${row.stale_item_count}`);if(row.date_mismatch_samples?.length)details.push(`日期衝突：${row.date_mismatch_samples.slice(0,2).join("、")}`);return `<article class="panel content"><h3>${escapeHtml(row.label||row.file)}${row.critical?' <small class="tag">核心</small>':''}</h3><p>${escapeHtml(row.file)}</p><p>${details.map(escapeHtml).join(" · ")}</p><p class="muted">${escapeHtml(reasons(row))}</p><span class="${css(status)}">${escapeHtml(label(status))}</span></article>`}).join("")||'<div class="empty">健康索引尚未產生；不會改用大量歷史資料硬撐畫面。</div>';
})();
