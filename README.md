# 全球市場即時雷達 v6

公開網址：

```text
https://qwe70542asd-blip.github.io/market-event-radar/
```

這是一個以 **Asia/Taipei 台灣時間** 顯示的投資事件工作台，整合市場代理行情、財報、央行、經濟數據、科技活動與相關報導。

## v6 重點

- 移除容易出現紅色驚嘆號的 TradingView 大型跨國行情表。
- 只保留一條較輕量的市場代理跑馬燈：EWT、TSM ADR、SPY、QQQ、SOXX、EWJ、EWY、FEZ、USDJPY、US10Y、VIX。
- 預設顯示未來 7 天事件板，同一天多事件會並排顯示。
- 滑鼠停留或鍵盤聚焦事件，可快速查看影響與相關報導；點擊可開啟完整詳情。
- 新增台股／AI、利率／通膨、科技財報、日本／亞洲焦點模式。
- 新增 `Ctrl + K` 快速搜尋與動作選單，並支援 `/` 聚焦事件搜尋。
- 篩選、焦點與顯示模式會保存在瀏覽器中。
- 新增公開新聞 RSS 整合，GitHub Actions 每 4 小時更新相關報導。
- 事件來源失效時保留上一次成功資料，避免整頁空白。

## 為什麼原本有紅色驚嘆號

原本的大型 TradingView Market Quotes 元件包含 `INDEX:TAIEX`、`NASDAQ:SOX`、`TVC:NI225`、`KRX:KOSPI`、`FTSE:UKX`、`EURONEXT:PX1` 等商品。部分交易所或商品只允許在 TradingView 本站查看，不允許第三方網頁嵌入；錯誤發生在跨網域 iframe 內，外層網站無法可靠偵測或修復。

v6 不再使用這個大型元件，改用可公開顯示的 ETF、ADR、匯率與風險指標作為市場代理，因此不再留下大片空白或紅色驚嘆號。

## 自動更新

### 市場事件

`.github/workflows/update-events.yml` 每天約台灣時間 08:15 執行：

1. 下載 BLS 行事曆。
2. 讀取 BEA 發布時程。
3. 嘗試讀取 Nasdaq 財報行事曆。
4. 合併 `data/manual_events.json`。
5. 更新 `data/events.json` 與 `data/seed.js`。

### 相關報導

`.github/workflows/update-news.yml` 每 4 小時執行：

1. 讀取未來 35 天高／中影響事件。
2. 透過 Google News RSS 搜尋相關公開報導。
3. 每個事件保留最多 3 則，並去除重複網址。
4. 更新 `data/news.json` 與 `data/news-seed.js`。
5. 抓取失敗時保留上一次成功資料。

## 啟用 GitHub Pages

1. **Settings → Pages**。
2. Source 選 **Deploy from a branch**。
3. Branch 選 `main`，資料夾選 `/ (root)`。
4. 按 **Save**。

請確認 **Settings → Actions → General → Workflow permissions** 已選 `Read and write permissions`。

## 本機預覽

```bash
python -m http.server 8000
```

開啟：

```text
http://localhost:8000
```

## 免責聲明

本網站僅整理公開市場資訊，不構成投資建議。行情為市場代理或延遲資料，事件與報導也可能臨時調整；交易前請再次確認官方公告、交易所與券商報價。

## v7 版更新重點

- 首頁改為月曆主版，不再以 7 天週板當主畫面。
- 資料健康縮成右側緊湊清單，避免大片空白。
- 移除首頁新聞區；點事件後進入 `event.html?id=...` 詳情頁再看介紹與相關報導。
- Hero 與統計卡縮小，提升資訊密度。


## v8 個人化與登入

- 訪客模式與 Google 登入介面。
- Firebase Authentication + Cloud Firestore 跨裝置同步骨架。
- 自選股票、收藏事件、通知權限、離線本機資料。
- 需依 `Firebase設定說明-v8.txt` 填入 Firebase 專案設定後，Google 登入才會啟用。


## v9 緊湊版與多來源新聞

- 首頁改為單一緊湊投資工作台。
- 登入／訪客入口固定於右上角，並顯示 v9 版本標籤。
- 新增獨立 `news.html` 多來源財經新聞頁。
- 新聞更新程式支援中央社、經濟日報、MoneyDJ 與多家台灣／國際媒體來源。
- 首頁僅保留三則新聞焦點，避免資訊過度分散。


## v9.2

- 修正手機帳號同步文字直排。
- 新增重要新聞 60 秒輪播跑馬燈。
- 新聞抓取改為每 15 分鐘，並在相關程式上傳時自動執行一次。



## v10.0 第一版

- 修復新聞資料已生成但前端仍顯示抓不到的快取問題。
- 新增共用新聞載入器、來源狀態與手動重試。
- 新增股票／ETF／基金投資組合與個人化新聞。
- 新增基金主題配對。
- 國際新聞使用正確語系搜尋，加入 White House、PMI／ISM、基金與 ETF 來源。



## v10.1 中文來源擴充

- 中文來源由原本約 6 家擴充為台灣媒體、台灣官方與香港繁體中文三大群組。
- 新聞頁預設中文優先，並新增來源群組篩選。
- 臺灣證券交易所 RSS 改為直接抓取。
- 延伸來源採四組輪替，每小時完成一輪，降低來源限流風險。



## v10.1.1 重複新聞修正

- 新聞不再只依網址去重。
- 加入標題正規化、來源尾綴移除與保守的近似標題比對。
- 同一新聞被不同媒體入口轉載時，優先保留官方或原始來源。
- 前端載入時再次去重，舊 JSON 也能立即改善。



## v10.2 全產業版

- 新聞與個人化不再以科技為中心。
- 加入金融、航運、傳產、機械、營建、消費、生技、能源、電信、觀光、汽車與農業分類。
- 首頁焦點新聞採跨產業多樣化選取。
- 美股財報追蹤擴展至各主要產業。


## v10.3

- 新聞分成股票、基金與虛擬貨幣，並加入幣類分類。
- 修正所有下拉選單文字不可見。
- 投資組合改用正式資產主檔，顯示名稱與代號。
- 首頁新增三大法人與台美日官方重要公告。
- 新增個股財報、穩健度、產業排名、重要指標與圖表詳情頁。



## v10.4 豐富事件月曆

- 月曆從純總經事件擴展到財報、月營收、法說會、股利方案、除權息、ETF 配息、股利發放及股票分割。
- TWSE／TPEx 除權息與主動型 ETF 配息採獨立分類。
- Nasdaq 財報不再刪除小型公司，並加入美股股利及股票分割。
- MOPS 公司重大訊息、法人說明會與月營收加入日曆。
- 月曆不再隱藏已經過一天的當月事件。
- 同一天大量事件會顯示分類統計與完整清單。
- v10.4 安裝包內含 144 筆事件，2026 年 8 月共有 113 筆初始事件。



## v10.4.1 日曆與資料連結修正

- 月曆改為固定高度的圓點、類別與數量，不再塞滿完整事件卡。
- 最近 96 小時的戰爭、制裁、關稅、災害與交易中斷消息可轉成突發市場事件。
- Google News RSS 編碼轉址改成安全的完整標題搜尋，修正新聞 404。
- 三大法人於週末與休市日自動顯示最近交易日。
- 修正官方市場資料工作流程未成功提交 announcements.json 的問題。
- 修正外資自營商重複計入自營商買賣超。



## v10.4.2 月曆可讀性調整

- 放大月曆日期、每日總件數、事件分類名稱與分類數量。
- 圓點稍微放大，提升深色背景下的辨識度。
- 桌面與手機格子高度小幅增加，仍維持精簡統計模式。



## v10.4.3 新聞原文連結

- 媒體官方 RSS 直接使用文章網址。
- Google News 項目嘗試解析出版社原文網址，結果寫入連結快取。
- 解析不到時才顯示精確搜尋備援，並在新聞卡清楚標示。
- 新聞卡新增「閱讀原文／搜尋原文」與「來源首頁」。
- 新增 Yahoo 股市最新、台股、國際與基金官方 RSS。
