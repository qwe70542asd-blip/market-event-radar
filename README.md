# 市場事件雷達 v11.4.29

本版整合首頁台股產業熱度卡、行情新鮮度提示，以及即時／歷史新聞共用的文章原圖擷取流程。

## 本版重點

- 原本的直式股票跑馬燈改為「台股產業熱度榜」。
- 每次顯示最多 5 個熱門產業，每個產業最多 2 檔代表股。
- 產業分類以 TWSE／TPEx 公司主檔、正式產業、公司名稱與營業範圍為準；新聞內容不再覆蓋正式產業。
- 同一檔股票在熱度榜只會出現一次。
- 產業強弱每 5 分鐘重算，代表股名單每 15 分鐘更新。
- 每張產業卡顯示產業漲跌、上漲／下跌家數、成交額，以及代表股價格、漲跌幅與入選原因。
- 行情區顯示實際資料時間，並依資料年齡標示「近即時」「最近行情」或「延遲」。
- 未部署 Worker 時不再把 GitHub 排程資料宣稱為即時行情。
- 即時新聞與 Google News 歷史回補資料合併後，統一再進入文章圖片擷取流程。
- 高影響、近期且缺圖的新聞優先抓取文章 OG Image、Twitter Image、JSON-LD、srcset 或內文主圖。
- 網站共用 Logo、預設 OG 圖、placeholder、icon 與過小圖片不會被當成新聞照片。
- 原圖確實不可用時才顯示本地主題備援圖。
- 全站資源與快取版本升級至 v11.4.29，避免舊版首頁與新聞卡繼續留在瀏覽器。

## 覆蓋部署

1. 解壓縮 ZIP。
2. 將包內全部檔案直接覆蓋 repository 根目錄。
3. 保留原本的 `.git` 資料夾。
4. GitHub Desktop Commit：`market-event-radar-v11.4.29`。
5. Push origin。
6. 等待 `Verify v11.4.29 app release` 與各新聞工作流程完成。

歷史新聞照片會在 Push 後的新聞 Actions 重新處理；壓縮包本身不會假裝已完成第三方網站的線上抓圖。

## 驗證結果

本包建立時已完成：

- 105 項自動測試
- 嚴格公開資料驗證
- Python 與 JavaScript 語法檢查
- 27 份 GitHub Actions YAML 解析
- 本機 HTTP 測試：7 個頁面、42 個引用資源
- 台積電、台灣大、台達電的產業分類抽查

## 行情更新限制

GitHub Actions 是排程備援，不保證每分鐘執行。真正的 30～60 秒行情仍需部署 `edge/market-live-worker.js`，並把 Worker 網址填入 `assets/runtime-config.js`。Worker 尚未設定時，頁面會誠實標示資料時間與延遲狀態。

資料僅供市場觀察，不構成投資建議。
