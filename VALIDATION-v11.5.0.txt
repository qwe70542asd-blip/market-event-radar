Market Event Radar v11.5.0 validation

Release type: Full Replacement / Compact Live Data Architecture
Version: v11.5.0

PASS repository cleanup
PASS 24 isolated live branches / single writer audit
PASS Python compileall
PASS JavaScript syntax (node --check)
PASS shell syntax (bash -n)
PASS 16 GitHub workflow YAML files
PASS security audit
PASS production static readiness v11.5.0
PASS strict public data validation
PASS frontend compact payload budgets (bundled fallback dataset)
  home-assets.json        5,046 / 800,000 bytes
  home-events.json       37,775 / 2,000,000 bytes
  tw-market-compact.json  7,486 / 2,000,000 bytes
  tw-chips-compact.json      807 / 4,000,000 bytes
PASS pytest: 326 passed
PASS HTTP smoke: 7 pages / 38 referenced assets
PASS live-source contract: external_warnings=5, exit 0
  (sandbox DNS cannot resolve TWSE/TPEx endpoints; no fabricated data used)

Browser smoke:
ENVIRONMENT BLOCKED. System Chromium launches, but localhost navigation is denied by the execution environment with ERR_BLOCKED_BY_ADMINISTRATOR. GitHub Hosted Runner remains the authoritative browser gate after push.

Architecture regression gates added:
- Home must not boot assets.json/events.json/tw-chips.json/dividend-history.json.
- Institutional page must not boot full tw-chips or Yahoo details.
- Portfolio/tw-market/stale guard/date alerts use compact contracts.
- Event page uses compact event archive first; full events archive is on-demand fallback only.
- Asset detail shards use 00-99 two-digit buckets; old 0-9 shard files removed.
- Unknown numeric industry taxonomy cannot render as a visible industry name.
- Legacy mobile-global-quick-nav injection system removed.
- Compact payload sizes are CI-enforced before publication.

Known live-source conditions are not hidden by this release:
- source-date mismatches remain degraded until upstream data catches up;
- stale Yahoo enrichment remains secondary and cannot override official current data;
- MOPS circuit-open remains degraded with last-known-good behavior;
- cross-source conflicts remain visible in data verification / health status.
