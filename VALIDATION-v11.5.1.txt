Market Event Radar v11.5.1 validation

Release type: Full Replacement / Runtime Stability + Compact Live Data Architecture
Version: v11.5.1

PASS repository cleanup / stale release guard
PASS 24 isolated live branches / single-writer audit
PASS Python compileall
PASS JavaScript syntax (node --check)
PASS shell syntax (bash -n)
PASS 16 GitHub workflow YAML files
PASS security audit
PASS production static readiness v11.5.1
PASS strict public data validation
PASS active bundled data metadata: 139 versioned JSON payloads all v11.5.1
PASS frontend compact payload budgets
  home-assets.json          3,473 /   800,000 bytes
  home-events.json         31,090 / 2,000,000 bytes
  tw-market-compact.json    5,034 / 2,000,000 bytes
  tw-chips-compact.json       636 / 4,000,000 bytes
PASS pytest: 333 passed
PASS HTTP smoke: 7 pages / 38 referenced assets
PASS live-source contract: external_warnings=5, exit 0
  (sandbox DNS cannot resolve TWSE/TPEx endpoints; no fabricated data used)

Runtime-stability regression gates:
- Homepage seed fallback is one bundled request instead of 19 individual seed scripts.
- Homepage external scripts are deferred; no parser-blocking external script remains.
- Identical in-flight GETs are deduplicated by canonical URL.
- First-paint branch mirror fanout is bounded; later refresh may use the wider fallback ladder.
- Homepage news boot is progressive and bounded to one worker with delay/stagger.
- focus/visibility market refreshes are throttled.
- stale-market guard does not force duplicate market/chips/snapshot fetches during first paint.
- legacy local/session/IndexedDB cleanup is deferred to load/idle instead of synchronously scanning all channels.
- Service Worker uses atomic v11.5.1 cache, cache-first versioned static assets, bounded network-first data/navigation, and no ignoreSearch:true fallback.
- Service Worker registration does not reload the page on controllerchange; old market-event-radar caches are purged.
- Actions release-version validation resolves VERSION.json rather than embedding the patch number.
- No patch / updater / overlay / one-click release artifact is required or bundled.

Browser smoke:
ENVIRONMENT BLOCKED. System Chromium launches, but localhost navigation is denied by the execution environment with ERR_BLOCKED_BY_ADMINISTRATOR. GitHub Hosted Runner remains the authoritative browser gate after push.

Known live-source conditions are not hidden by this release:
- source-date mismatches remain degraded until upstream data catches up;
- stale Yahoo enrichment remains secondary and cannot override official current data;
- MOPS circuit-open remains degraded with last-known-good behavior;
- cross-source conflicts remain visible in data verification / health status.
