# v11.4.17 — 六大指數連續日 K

## 顯示指數

`^TWII`、`^KS11`、`^N225`、`^IXIC`、`^SOX`、`^GSPC`。

`^TWOII` 已移除。

## 資料格式

每個 K 線標的在 `data/market-snapshot.json` 內保存：

- `candles[]`
- `date`
- `timestamp`（Yahoo 資料）
- `open`
- `high`
- `low`
- `close`
- `volume`
- `candle_source`
- `data_status`

## 來源優先級

1. 台灣加權日 K：TWSE 官方歷史資料。
2. 台灣加權即時報價：Yahoo Chart。
3. 韓國、日本與美國指數日 K：Yahoo Chart。
4. 單一來源失敗：保留該標的上一次成功資料。
