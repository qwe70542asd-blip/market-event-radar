# v11.4.44

Reliability and repository-cleanup release.

- Rebuilt Taiwan market session handling so quote date and turnover-history end date are independent.
- Rejects partial TWSE/TPEx refreshes and cross-exchange session conflicts; retains last-known-good instead.
- Removes 15 duplicate workflow publishers; one authoritative writer now owns each live branch.
- Adds generic workflow-publisher conflict audit.
- Replaces manual stale-file lists with version-aware repository cleanup.
- Prevents the 5-minute market-core scheduler from cancelling a healthy in-flight run.
- Adds regression coverage for quote-session coherence, date regression and per-exchange completeness.
