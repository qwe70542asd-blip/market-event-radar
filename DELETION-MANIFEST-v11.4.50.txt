Market Event Radar v11.4.50 deletion/synchronization manifest

This is a correctness-first cleanup release.

Removed/replaced runtime artifacts:
- assets/v11.4.49-overrides.css -> assets/v11.4.50-overrides.css
- release-specific service-worker registration version
- exact frontend/Worker patch-version coupling
- homepage dependency on the full multi-megabyte events archive
- fake 09:00 display for date-only events
- release-specific calendar localStorage key
- stale workflow producer version constants

The cleanup engine also removes old versioned tests/verifiers, old deletion manifests,
old v*-overrides.css, apply_v*.py leftovers, redundant standalone publishers, and nested copies.
