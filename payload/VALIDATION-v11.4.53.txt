Market Event Radar v11.4.53 validation targets

1. scripts/publish_data_branch.sh
   - bash -n syntax gate
   - executable local bare-repository regression test
   - VERSION.json parsing no longer uses nested shell quoting

2. Taiwan stale-data fail closed
   - compares tw-market trading_date with verified ^TWII session_date
   - if market data is older, homepage stops showing bullish/bearish, foreign-flow and volume conclusions

3. Cloudflare KV budget
   - cron reduced from every minute to every five minutes
   - Cache API stores hot snapshot and kline responses
   - KV stores snapshot LKG checkpoint at most every 15 minutes
   - kline path performs no new KV writes

4. Release hygiene
   - one-click installer deletes all old version manifests, validations and override CSS
   - service-worker cache version is bumped to v11.4.53
